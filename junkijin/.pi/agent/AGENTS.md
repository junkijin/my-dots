Compatibility:
- Treat code changes as breaking changes by default.
- Do not add fallback logic unless the user asks for it.
- Avoid compatibility shims, aliases, and legacy branches by default.
- If backward compatibility is needed, explain why before adding it.
